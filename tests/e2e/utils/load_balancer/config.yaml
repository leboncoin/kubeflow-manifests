cluster:
  name: <<CLUSTER_NAME>>
  region: eu-west-1
kubeflow:
  alb:
    serviceAccount:
      name: aws-load-balancer-controller
      namespace: kube-system
      policyArn: <<ROLE_ARN_KUBECONTROLLER>>
route53:
  rootDomain:
    hostedZoneId: <<HOSTED_ZONE_ID>>
    name: <<ROOT_DOMAIN>>
  subDomain:
    hostedZoneId: <<HOSTED_ZONE_ID>>
    name: <<SUBDOMAIN>>
